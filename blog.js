fetch("/api/blog").then(e=>e.json()).then(e=>{const t=document.getElementById("blogPosts");e.length?t.innerHTML=e.map(e=>`<article class="blog-post"><h2><a href="blog-post.html?id=${e.id}">${e.title}</a></h2><p><em>${new Date(e.date).toLocaleDateString()}</em></p>${e.image?`<img src="/uploads/${e.image}" alt="${e.title}" class="blog-image">`:""}<div>${e.content.substring(0,200)}${e.content.length>200?"...":""}</div></article>`).join(""):t.innerHTML="<p>No blog posts yet.</p>"}).catch(()=>{document.getElementById("blogPosts").innerHTML="<p>Could not load blog posts.</p>"});